apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-demo
spec:
  type: {{ .Values.service.type }}

  selector:
    app: {{ .Release.Name }}-demo

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http