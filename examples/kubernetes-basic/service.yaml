apiVersion: v1
kind: Service
metadata:
  name: enterprise-demo
  namespace: enterprise-demo

spec:
  type: ClusterIP

  selector:
    app: enterprise-demo

  ports:
    - name: http
      port: 80
      targetPort: http